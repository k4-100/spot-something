import { Box } from "@mui/material";

const SoundControls: React.FC = () => {
  return (<Box sx={{
  }}>
    <Box sx={{
      height: "100%",
      background: "red"
    }}>
    </Box>
  </Box>);
};

export default SoundControls;
