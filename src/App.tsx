import { useEffect, useState } from "react";

function App() {
  const [name, setName] = useState("sa")

  useEffect(() => {
  }, []);

  return (
    <div className="container">
      s
    </div>
  );
}

export default App;
